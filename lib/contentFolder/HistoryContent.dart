import 'package:flutter/material.dart';

class HistoryContantPage extends StatelessWidget {
  const HistoryContantPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}